<%= link_to gravatar_for(current_user, size: Settings.gravatar.list), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t(".view_profile"), current_user %></span>
<span><%= pluralize current_user.microposts.count, t(".post_count") %></span>
